# runlens-cli

Command-line interface for RunLens. Subcommands: `init`, `record`, `list`, `show`, `verify`, `compare`, `bug-report`, `export`, `import`, `investigate`, `roll`, `mcp`, `redactions`, `where`.

Output is human-readable tables by default; `--json` switches all subcommands to JSON.
